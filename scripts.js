










// Existing JavaScript code});    header.style.height = window.innerHeight * 0.1 + 'px';    const header = document.querySelector('.header');    // For example, adjust the height of a specific element    // Add any JavaScript needed to handle responsive changeswindow.addEventListener('resize', function() {// Example: Adjust elements based on window resize// Existing JavaScript code